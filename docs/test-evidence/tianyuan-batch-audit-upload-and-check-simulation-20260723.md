# 查证资料索引上传与查证核对情况批量模拟测试

日期：2026-07-23

## 测试目标

在当前资产基础法底稿科目中，模拟批量执行：

1. 在“查证资料索引”中上传测试文件。
2. 扫描已落库的资料索引。
3. 批量填写“查证核对情况”。
4. 保存并回读。

## 当前页面

- 天源项目 ID：`165353602809858`
- 公司 ID：`165353602809933`
- URL 科目参数：`C5-9`
- 当前 Sheet：`长期待摊费用`
- `查证资料索引` 动态定位：`N` 列
- `查证核对情况` 动态定位：`O` 列
- 测试文件：`tianyuan-batch-audit-index-test-20260723.pdf`

## 初始扫描

- 扫描数据行：21
- 有“查证资料索引”的行数：0
- 需要填写“查证核对情况”的行数：0

## 上传测试

### 第 2 行

- 目标单元格：`N2`
- 文件注入成功。
- `/ty/api/attach/upload`：HTTP 200，业务 code 200。
- `/ty/api/assignment_draft/cell_file/classify_upload`：HTTP 200，业务 code 200。
- `/ty/api/assignment_draft/save`：HTTP 200，业务 code 200。
- `N2` 回读批次号：`0eb9b2cc-3b1e-47b1-8b89-123c361c51a3`。
- 结论：第 2 行资料索引上传、分类、保存和回读成功。

### 第 3 行

- 目标单元格：`N3`
- 第一次按“摊销明细表”分类上传：
  - `/attach/upload` 返回成功；
  - `/cell_file/classify_upload` 返回业务 code 500，`系统异常`；
  - 未触发正式底稿保存；
  - `N3` 未形成资料索引回读。
- 第二次按分类序号 0 上传：
  - `/attach/upload` 返回成功；
  - `/cell_file/classify_upload` 仍返回业务 code 500，`系统异常`；
  - 未触发正式底稿保存；
  - `N3` 未形成资料索引回读。
- 结论：第 3 行附件入库请求发生，但分类批次生成失败，不算上传保存成功。

## 批量填写核对情况

- 上传后扫描结果：
  - 有资料索引的行数：1；
  - 需要填写核对情况的行数：1；
  - 候选行：第 2 行。
- 执行批量填写：
  - 目标值：`不一致`。
  - 更新行：第 2 行。
  - `POST /ty/api/assignment_draft/save`：HTTP 200，业务 code 200。
  - 保存 requestId：`8dcc45c816007722898637723c5beeaa`。
  - `O2` 回读 text/value 均为 `不一致`。
  - `readbackConsistent=true`。

## 最终扫描

- 有“查证资料索引”的行数：1。
- 需要填写“查证核对情况”的行数：0。
- 第 2 行：
  - `N2`：`0eb9b2cc-3b1e-47b1-8b89-123c361c51a3`
  - `O2`：`不一致`

## 发现的问题

- 第 3 行分类接口连续返回 `系统异常`，说明不能把 `/attach/upload` 成功视为上传保存成功。
- 失败时弹窗可能保留文件输入状态，后续保存可能触发多个上传/分类请求。
- 已补强页面适配器：
  - 分类名称匹配改为使用弹窗输入描述；
  - 正式上传前尝试清空非目标分类的文件输入；
  - 空 `rowNumbers: []` 不再被误判为用户指定空范围。

## 安全结果

- 未读取或记录 Cookie、Authorization、密码、验证码或 token。
- 本地临时测试 PDF 已删除。
- 未开放任意 JavaScript。
- 批量写入仅限“查证核对情况”字段。

## 结论

小批量模拟测试部分通过：

- 成功验证第 2 行“测试附件上传 -> 资料索引落库 -> 批量填写核对情况 -> 保存 -> 回读一致”。
- 第 3 行在分类批次生成阶段被系统异常阻断，未保存到底稿；该失败验证了插件的分段门禁有效。

## 2026-07-24 追加排查

用户截图显示 `BATCH_UPLOAD_ALL_FAILED`，判定为本次批量没有任何一行完成正式成功闭环，不能作为批量上传成功证据。

已发现并修复两个代码层问题：

- `content.js` 与 `page_adapter.js` 的 `ADAPTER_VERSION` 不一致，可能导致新版执行逻辑未被 content script 接收。
- `tianyuan.batch_upload_audit_attachments` 工具 schema 缺少 `procedureText`，批量动作无法显性传入“查证类核实程序”前置字段。

已补强分类失败诊断：失败结果会额外返回前置程序处理结果和上传弹窗文本。追加版本为 `2026-07-24-page-tree-mirror-v19-upload-residual-guard`，并增加上传弹窗残留文件硬保护。

### 第 5 行追加复测

- 目标单元格：`N5`。
- `M5` 查证类核实程序回读已为 `合同协议`。
- `/assignment_draft/save` 曾返回业务成功，用于保存前置字段状态。
- `/attach/upload` 返回业务成功，但同一动作出现多次附件入库请求。
- `/cell_file/classify_upload` 连续返回业务 `code:500`、`msg:"系统异常"`。
- 未触发正式底稿保存，`N5` 未形成资料索引回读。
- 结论：第 5 行仍不是上传保存成功；问题集中在上传弹窗残留/重复上传与天源分类接口异常，不能继续盲目批量执行。

下一轮需要重新加载扩展，使 v19 残留保护生效；先做只读扫描确认适配器版本为 v19，再决定是否继续单行复测。

## 2026-07-24 换科目批量上传成功

### 当前页面

- 当前 Sheet：`其他应收款-其他应收款`
- `查证类核实程序`：`P` 列
- `查证资料索引`：`Q` 列
- `查证核对情况`：`R` 列
- 页面适配器版本：`2026-07-24-page-tree-mirror-v19-upload-residual-guard`

### 预演

- 目标单元格：`Q2`
- 上传分类弹窗可打开。
- 识别到分类：`凭证`、`合同`、`期后回款`、`关联`。
- 预演未注入文件、未上传、未保存。

### 批量上传

- 执行行：第 2 行、第 3 行。
- 分类：`凭证`。
- 测试文件：`tianyuan-real-batch-upload-test-20260724.pdf`。
- 第 2 行：
  - `P2` 前置程序先写入 `函证`，上传后页面回读为 `凭证`。
  - `/attach/upload` 业务成功，附件 ID `169418769170433`。
  - `/cell_file/classify_upload` 业务成功，批次号 `c30b34ed-0db3-4e31-b6ea-964cbbcadb12`。
  - `/assignment_draft/save` 业务成功。
  - `Q2` 回读为 `c30b34ed-0db3-4e31-b6ea-964cbbcadb12`。
- 第 3 行：
  - `P3` 前置程序先写入 `函证`，上传后页面回读为 `凭证`。
  - `/attach/upload` 业务成功，附件 ID `169418818715650`。
  - `/cell_file/classify_upload` 业务成功，批次号 `8a3053bb-191a-4b3c-9272-3adbfac91815`。
  - `/assignment_draft/save` 业务成功。
  - `Q3` 回读为 `8a3053bb-191a-4b3c-9272-3adbfac91815`。

### 结论

- 当前科目 `其他应收款-其他应收款` 的第 2、3 行完成真实批量上传闭环。
- `summary.successRows=2`、`failedRows=0`。
- 由于测试文件不是业务证据，后续若填写“查证核对情况”，应填写 `不一致`，不能填写 `一致`。
- 发现扫描口径问题：空资料索引单元格的 `tag:{isClear:true}` 不能视为已有资料。已在本地修复为 `2026-07-24-page-tree-mirror-v20-index-clear-tag-fix`，重新加载扩展后生效。

## 2026-07-24 批量填写查证核对情况成功

- 执行范围：当前 Sheet `其他应收款-其他应收款` 第 2、3 行。
- 填写字段：`查证核对情况`，动态定位为 `R` 列。
- 填写值：`不一致`。
- `/assignment_draft/save`：HTTP 200，业务 code 200，requestId `06255d36352cdf33ad65f61e18263e42`。
- `R2` 回读：`不一致`。
- `R3` 回读：`不一致`。
- 最终扫描：
  - `Q2`：`c30b34ed-0db3-4e31-b6ea-964cbbcadb12`，`R2`：`不一致`。
  - `Q3`：`8a3053bb-191a-4b3c-9272-3adbfac91815`，`R3`：`不一致`。
  - `rowsNeedingCheck=0`。

结论：当前科目两行已完成“测试附件上传 -> 资料索引落库 -> 查证核对情况填写 -> 保存 -> 回读”的小批量闭环。

## 2026-07-24 测试数据清理

- 清理范围：当前 Sheet `其他应收款-其他应收款` 第 2、3 行。
- 清理字段：
  - `P2/P3`：查证类核实程序；
  - `Q2/Q3`：查证资料索引；
  - `R2/R3`：查证核对情况。
- 清理前校验批次号：
  - `Q2=c30b34ed-0db3-4e31-b6ea-964cbbcadb12`；
  - `Q3=8a3053bb-191a-4b3c-9272-3adbfac91815`。
- `/assignment_draft/save`：HTTP 200，业务 code 200，requestId `4937b289ef3be87ee5c446afbb88dc3b`。
- 回读结果：
  - `P2/Q2/R2` 均为空；
  - `P3/Q3/R3` 均为空。
- 最终扫描：
  - `rowsWithIndex=0`；
  - `rowsNeedingCheck=0`。
- 本地测试 PDF 已从 `/tmp/tianyuan-workbench-test/` 删除。

说明：本次清理清除了底稿单元格引用和本地测试文件；此前 `/attach/upload` 已入库的测试附件如需从附件库物理删除，需要天源提供明确删除接口，当前插件未猜测调用删除接口。
