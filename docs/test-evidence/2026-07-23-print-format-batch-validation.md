# 打印格式批处理验证

日期：2026-07-23

## 来源核对

- 申报表 Skill 压缩包 SHA-256：`7f19f965529e1603f05ae0bf23422eb9209e44b5a8644e0e7ebad5f084c0335a`
- 明细表 Skill 压缩包 SHA-256：`0d69dc4a324c2522ad1f8e8be334a3eb383ed15a44f761d95134df01ccf72431`
- 集成后申报表脚本 SHA-256：`678ef44193eececc8637fa8d99c02c01c87c776a41b597b10e2732437c8ef81c`
- 集成后明细表脚本 SHA-256：`94a505cfc809e1fbcd17927af79baec2d790323a65d55330360880f092b85f0e`

## 环境

- Python：`3.9.6`
- openpyxl：`3.1.5`
- 运行依赖安装目录：`~/.tianyuan-workbench/dependencies/天源评估系统/print-format-skills/`

## 静态验证

- Native Host 和侧栏 JavaScript 通过语法检查。
- 两个 Python 脚本通过 `py_compile`。
- Manifest JSON 解析通过，版本为 `0.3.0`。
- HTML 共 133 个 ID，JavaScript 共定义 133 个元素引用，未发现缺失 ID。
- `git diff --check` 通过。

## 工作簿测试

使用本地生成的无业务数据样本工作簿验证，没有处理用户正式文件：

- 文件夹模式发现并处理 2 个明细表；
- 在源文件夹成功生成 2 个 `-打印版.xlsx` 副本；
- 新目录模式成功生成 1 个申报表打印版；
- 覆盖模式成功处理 1 个明细表；
- 覆盖模式源文件 inode 发生变化，确认采用临时文件原子替换；
- 覆盖模式未遗留备份文件或隐藏临时文件；
- 所有输出均通过 `unzip -t` 完整性检查。

## 安装验证

- 项目 Native Host 与本机安装副本 SHA-256 一致。
- 两个 Skill 脚本与本机安装副本 SHA-256 一致。
- 使用已安装 Native Host launcher 成功处理 1 个申报表样本并通过完整性检查。

## 界面验证

Chrome Headless 静态预览确认：

- 首页显示 6 个功能模块；
- 明细表和申报表打印格式页面均显示文件选择、文件夹选择、输出方式、进度条、日志和证据区；
- 新位置输入区默认隐藏，选择对应输出方式后显示；
- 日志和证据默认折叠。

## 验证边界

尚未处理真实天源导出工作簿。重新加载扩展后，建议先用工作副本验证打印预览，再使用覆盖源文件模式。
