# 2026-07-23 科目名称路径匹配 v7 静态验证

## 目标

修复页面左侧已经显示、但插件清单仍漏掉的科目，重点覆盖：

- `其他应付款 -> 其他应付款`
- 带完整路径或名称后缀的 MCP 科目
- 页面树不完整时仍应保留页面明确出现的科目

## 修复内容

- `extension/src/sidepanel/sidepanel.js`
  - 科目匹配不再只比较单个 `name`。
  - 同时比较科目名称、父级名称、路径、原始 MCP 名称字段和路径片段。
  - 支持名称后缀匹配，覆盖 `其他应付款-其他应付款` 等返回形式。
  - 页面树不完整时，将 MCP 显示科目与页面明确匹配到的科目合并，避免页面科目被吞掉。
  - 证据 JSON 新增 `fallbackCount`，用于核查回退合并数量。

## 验证结果

- `extension/src/sidepanel/sidepanel.js` 语法检查通过。
- `extension/src/injected/page_adapter.js` 语法检查通过。
- `extension/src/content/content.js` 语法检查通过。
- `extension/manifest.json` JSON 解析通过。
- 本地匹配样例通过：`其他应付款-其他应付款`、`电子设备`、`其他流动资产`。

## 验证边界

仍需重新加载未打包扩展，在真实天源页面点击“加载科目”，核对 `其他应付款`、`其他流动资产`、`电子设备` 等页面显示科目是否进入清单。
